//
//  ContentView.swift
//  WellNest
//
//  Created by csuftitan on 4/16/24.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("I live in a constant state of agony")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
